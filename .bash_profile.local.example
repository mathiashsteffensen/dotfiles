# Machine-specific login-shell settings.
# Copy to .bash_profile.local, then customize. The .local file is ignored by Git.

# if [ -f "$HOME/google-cloud-sdk/path.bash.inc" ]; then
#     . "$HOME/google-cloud-sdk/path.bash.inc"
# fi

# Optional: Linear tools for Pi. Create a scoped key at:
# https://linear.app/settings/account/security
# export LINEAR_API_KEY="lin_api_..."
